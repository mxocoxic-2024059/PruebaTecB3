# Logo interno

Coloca el logo completo definitivo en:

```text
frontend/src/assets/taskflow-logo.png
```

El componente `Brand` conserva el espacio visual y muestra el nombre **TaskFlow** hasta que el recurso sea añadido. Después puede importarse este archivo desde `components/ui/Brand.jsx`.
